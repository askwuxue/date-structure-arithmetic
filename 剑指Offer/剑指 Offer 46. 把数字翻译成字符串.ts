function translateNum(num: number): number {
  const str: string = String(num)
  let p = 0, q = 0, r = 1
  const len = str.length
  // for (let i = 0; i < len; ++i) {
  //   p = q
  //   q = r
  //   r = 0
  //   r += q
  //   if (i === 0) {
  //     continue
  //   }
  //   const pre: string = str.substr(i - 1, i + 1)
  //   if (parseFloat(pre) < )
  // }
};